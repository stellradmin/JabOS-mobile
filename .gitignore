# Expo
.expo/
expo-env.d.ts
dist/
web-build/

# EAS Build
eas-build-pre-upload.tar.gz
eas-build-local-nodejs/

# Dependency directories
node_modules/

# Environment variables - ALL environment files ignored for security
.env
.env*
.env*.local
.env*.development
.env*.test
.env*.production
.env.stripe
.env.local
.env.production
.env.test
!example.env
!example.env.local
!*.env.example

# Build outputs
build/
android/app/build/
android/build/
android/.gradle/
ios/build/
ios/Pods/
ios/DerivedData/
*.xcuserdatad

# Firebase config files (if they contain sensitive info and are not templates)
# These are often placed inside /ios and /android folders which are already ignored below
# but adding them here for explicitness if they exist at the root.
GoogleService-Info.plist
google-services.json

# Android specific
*.keystore
*.jks
android/gradle/
android/app/src/debug/
android/app/src/release/

# iOS specific
# No specific iOS patterns needed beyond what's covered by /ios/

# macOS
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Original entries from expo-cli
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli
# expo-env.d.ts # Already covered above
# @end expo-cli

/ios/
/android/

# Keep these if they are intentionally versioned, otherwise remove/comment out
# /android # If you manage native code directly and want to commit it
# /ios # If you manage native code directly and want to commit it

# Production credentials and sensitive files (CRITICAL SECURITY)
credentials/
!credentials/README.md
!credentials/*.template
!credentials/*.json.template

# Deployment artifacts
deployment_summary_*.md
*.backup_*

# Native certificates and keys
*.p8
*.p12
*.key
*.mobileprovision
*.pem
*.der
*.cert
*.crt

# Store-specific files
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Coverage and testing
coverage/
*.lcov
.nyc_output
junit.xml

# Temporary files
tmp/
temp/
*.tmp

# Security artifacts
.artifacts/
gitleaks-report.json
security-summary.md
bundle-report.html
eslint-report.json
.expo-dist/

# Bundle analysis
bundle-stats/
source-map-explorer-results/
